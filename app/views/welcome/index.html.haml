.header
  .header-inner
    %a.logo{href:'/'}
      %h1 Dossier
    .header-partition
      - if session[:fb_login]
        .fb-sign-out
          Log Out
      - else
        .fb-sign-in
          Log In
    .header-partition
      .linked-sign-in
        Log In
        %script{type:"IN/login", 'data-onAuth' => "showInSelect"}
      .linked-sign-out.hidden
        Log Out

.container
  .sub-header
    .fb-select-container

      - if @events

        .fb-logo.select-logo
        %select.events-selector
          %option Events you're going to!
          - @events.each do |event|
            %option{:value => event["id"]}
              = "#{event['start_time'].to_datetime.strftime("%B %d, %Y")}  |  #{event['name']}"
        %select.change-rsvp-selector
          %option{:value => nil} RSVP
          %option{:value => "attending"} Going
          %option{:value => "declined"} Not-Going
          %option{:value => "maybe"} Maybe

        %br

      - if @events_invited || @events_maybe
        .fb-logo.select-logo
        %select.events-invited-selector
          %option Events you're not attending
          - @events_invited.each do |event|
            %option{:value => event["id"]}
              = "#{event["start_time"].to_datetime.strftime("%B %d, %Y")}  |  #{event["name"]}"

          - @events_maybe.each do |event|
            %option{:value => event["id"]}
              = "#{event["start_time"].to_datetime.strftime("%B %d, %Y")}  |  #{event["name"]}"

        %select.set-rsvp-selector
          %option RSVP
          %option{:value => "attending"} Going
          %option{:value => "declined"} Not-Going
          %option{:value => "maybe"} Maybe

  .event-container
    - if current_user
      .intro-text
        %h2 Select what you want to be briefed on
        %br
        %p Make a selection above to see who is going to an event or simply browse everyone in your circles
        %br
        %p Feel free to change your RSVP status with click of a button
    - else
      .intro-text
        %h2
          Be good with faces
          %em and
          names
        %br
        %p Whose name do you actually know?
        %br
        %br
        %hr
        %br
        %br
        %h2
          Make a quick decision about a party
        %br
        %p Just seeing the faces of other invitees may help inform your decision about whether you want to attend a particular event.
        %br
        %br
        %hr
        %br
        %br
        %h2
          Be informed
        %br
        %p Play a game to memorize the other guests names and/or job titles.
        %br
        %br
        %hr
        %br
        %br
        %h2
          Social integration
        %br
        %p Login with Facebook and LinkedIn to browse events, colleagues, friends, and maybe potential employers.
