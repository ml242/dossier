import {
  formatTime
} from 'dossier/helpers/format-time';

module('TimeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatTime(42);
  ok(result);
});
